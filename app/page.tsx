export default function Home() {
  return (
    <div>
      {/* Lot Size = (Account Size * Risk%) / (Stop Loss in Pips * Pip Value).  */}
      <button>Lot size calculator</button>
      <h1>presets</h1>
      <input type="text" name="" id="" />
    </div>
  );
}
